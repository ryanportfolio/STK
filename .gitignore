# Scratch space: labs, repro scripts, throwaway tooling (see CLAUDE.md)
.tmp/

# Per-machine Claude Code settings
.claude/settings.local.json

# Env / secrets
.env
.env.local
.env.*.local

# Dependencies / build output
node_modules/
dist/
build/

# Rust
/target/
.vendor/

# OS noise
Thumbs.db
.DS_Store

# Local offline build only (never committed)
.cargo/
